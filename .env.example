# Base de données (Neon — injecté automatiquement par l'intégration Vercel)
DATABASE_URL=

# Better Auth
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth (Google Cloud Console)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Mapbox (mapbox.com — clé publique, préfixe NEXT_PUBLIC_)
NEXT_PUBLIC_MAPBOX_TOKEN=

# Resend (resend.com — emails transactionnels)
RESEND_API_KEY=
